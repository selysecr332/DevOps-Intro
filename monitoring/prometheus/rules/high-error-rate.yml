groups:
  - name: quicknotes
    rules:
      - alert: HighErrorRate
        expr: |
          (
            sum(rate(quicknotes_http_responses_by_code_total{code=~"4..|5.."}[5m]))
            /
            sum(rate(quicknotes_http_requests_total[5m]))
          ) > 0.05
        for: 5m
        labels:
          severity: page
        annotations:
          summary: QuickNotes HTTP error ratio exceeds 5% for 5 minutes
          runbook_url: docs/runbook/high-error-rate.md
